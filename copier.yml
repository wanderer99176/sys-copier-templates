# 根目录路由配置
_min_copier_version: "9.3.0"

# 让用户选择模版
template_choice:
  type: str
  help: "请选择项目架构模版"
  choices:
    - py-fastapi-next
    - py-fastapi-react

# 核心魔法：根据选择动态跳转子目录
_subdirectory: "templates/{{ template_choice }}"
